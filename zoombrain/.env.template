# Environment Configuration
# Copy this file to .env and fill in your values

# ============= Email Configuration =============
# Required for sending meeting summaries and sentiment notifications
SENDER_EMAIL=your-email@gmail.com
SENDER_PASSWORD=your-app-password
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587

# ============= OpenAI Configuration (Optional) =============
# If you want to use OpenAI for better summaries
# Otherwise, the system will use extractive summarization
OPENAI_API_KEY=your-openai-api-key

# ============= Google Calendar Integration (Optional) =============
# For creating calendar events from detected dates
GOOGLE_CREDENTIALS_PATH=path/to/google-credentials.json
GOOGLE_CALENDAR_ID=primary

# ============= Slack Integration (Optional) =============
SLACK_WEBHOOK_URL=https://hooks.slack.com/services/YOUR/WEBHOOK/URL
SLACK_BOT_TOKEN=xoxb-your-bot-token

# ============= Microsoft Teams Integration (Optional) =============
TEAMS_WEBHOOK_URL=https://your-team.webhook.office.com/webhookb2/YOUR/URL

# ============= Application Settings =============
# Uncomment and modify if needed
# APP_HOST=0.0.0.0
# APP_PORT=8000
# DEBUG_MODE=False
